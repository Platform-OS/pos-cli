require "spec_helper"

describe MarketplaceKit do
  it "has a version number" do
    expect(MarketplaceKit::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
