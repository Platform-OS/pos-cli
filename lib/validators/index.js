const existence = require('./existence');
const url = require('./url');
const email = require('./email');
const directoryExists = require('./directoryExists');
const directoryEmpty = require('./directoryEmpty');

module.exports = {
  existence,
  url,
  email,
  directoryExists,
  directoryEmpty
};
